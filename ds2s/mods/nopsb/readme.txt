~ nopsb ~
no press start button

~ RE ~
scan for changed values
filter down either by quit to menu or restart
to ~500 values
batch change values to previous
once it led to unability to press start
track the exact value
FOWA
